---
name: functionality-first market plan
overview: Keep the current UI shell intact while the repo moves through a Prediction-Hunt-only plumbing stage that simplifies provider code, stabilizes the app contract, and defers deeper provider composition to a later phase.
todos:
  - id: define-ui-direction
    content: Keep the current dashboard, gallery, and market workspace as the approved baseline UI for now.
    status: completed
  - id: build-mock-domain
    content: Stabilize frontend domain models and mock data so they continue to backstop the app contract while Prediction Hunt plumbing is tightened.
    status: completed
  - id: ship-ui-surface-v1
    content: Treat the current three primary UI surfaces as the baseline and only make targeted UX changes when functionality demands them.
    status: completed
  - id: harden-data-boundaries
    content: Keep repository boundaries, loading states, and backend-owned route contracts stable while the provider layer is simplified.
    status: in_progress
  - id: introduce-fastapi
    content: Keep the FastAPI backend as the single app-facing contract layer for dashboard, markets, detail, and events.
    status: completed
  - id: implement-core-engine
    content: Add richer ingestion, event detection, signal retrieval, and grounded copilot workflows only after Prediction Hunt plumbing is stable.
    status: pending
  - id: add-canvas-last
    content: Implement canvas mode only after the canonical market/event workflow and data contracts are stable.
    status: pending
isProject: false
---

# Superseded

This plan is kept only as historical context.

The active product direction is now topic-centric and lives in [pivot.plan.md](pivot.plan.md).

# Development Plan

## Current Direction

This should still feel like a research workstation, not a trading terminal and not a generic AI dashboard.

The current working assumption is:

- keep the present UI shell
- keep the frontend/backend market contract stable
- use Prediction Hunt as the only active provider during plumbing
- defer Polyrouter composition and deeper intelligence workflows until after the contract and provider cleanup are stable

## Plumbing Stage

```mermaid
flowchart TD
    currentUi[CurrentUiShell] --> appContract[StableAppContract]
    appContract --> phOnly[PredictionHuntOnlyProvider]
    phOnly --> logging[LightweightLogging]
    logging --> nextPhase[PolyrouterAndCoreWorkflowsLater]
```

During this stage:

- `/`, `/markets`, and `/markets/[marketId]` remain the primary product surfaces
- the backend remains the only app-facing data boundary
- mocks can remain as a fallback, but Dome and Kalshi no longer define the main runtime path
- internal tooling is reduced to the Prediction Hunt desk only

## Next Slice To Execute

- keep the market contract stable in `frontend/lib/market-types.ts` and `backend/app/models/market.py`
- keep `frontend/lib/repositories/markets.ts` as the only frontend path for core market data
- keep `backend/app/services/markets.py` as the thin app-facing mapping layer over Prediction Hunt
- keep UI edits narrow and functional while the provider and backend boundaries are being simplified

